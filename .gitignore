# Dependencies
node_modules/

# Frontend build
frontend/dist/
frontend/.wrangler
frontend/.output
frontend/.vercel
frontend/.netlify
frontend/.vinxi

# Backend build & temp
backend/tmp/
backend/*.exe
backend/*.dll
backend/*.so
backend/*.dylib

# Environment files
.env
.env*.local
.env.dev
.env.prod
.env.prod.example
# Holds ADMIN_SECRET, the Turso token and, once live, the Plaid secret.
# .env.vps.example is committed; the real file must never be.
.env.vps
.env.secrets
*.secrets

# Docker compose overrides
compose.override.yaml
compose.prod.yaml

# IDE
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
.project
.classpath
*.launch
.settings/

# CI/CD
.cicd/*.log
.cicd/.webhook_secret

# Lock files (keep only package-lock.json)
pnpm-lock.yaml
yarn.lock
